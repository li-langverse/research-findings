# Placeholder — replace with harness-derived rows

```bash
cd lic
python3 benchmarks/harness/bench.py --tier 2 --only md_lennard_jones
```

Paste `max |ΔE/E|` per (N, dt) into the whitepaper grade matrix after runs complete.
